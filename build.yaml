---
name: "User Management"
guid: "670167bd-e7f8-4549-98e2-5ab2e11bc89f"
version: "12.0.0.0"
targetAbi: "12.0.0.0"
framework: "net10.0"
overview: "Group policy templates, display and playback preferences, account lifecycle, password hygiene, and easier user onboarding for Jellyfin."
description: >
  User Management provides user groups and password hygiene to accounts that already exist on your
  Jellyfin server. Define group templates that manage both permissions and display and playback
  preferences with live sync, set account expiry and auto-disable inactive users, and enforce password
  requirements. Use these groups to send user-managed, and protected invite links.
category: "General"
imageUrl: "https://raw.githubusercontent.com/JPKribs/jellyfin-plugin-usermanagement/refs/heads/master/Jellyfin.Plugin.UserManagement/Assets/Logo.png"
owner: "JPKribs"
artifacts:
- "Jellyfin.Plugin.UserManagement.dll"
changelog:
  features:
  - Invites can target the default group instead of a fixed one
  - The invite group list marks which group is the default
  - Group expiry and inactivity disabling now write activity log warnings
  fixes:
  - A member promoted to administrator is reverted to their original authentication provider
  - Administrators are dropped from group member lists on save
  - Users deleted from Jellyfin no longer linger in groups or enrollment records
  - A new user could land in no group when a save ran at the same moment
  - The Inactive card counts each member against their own group's window
  - Invite usage counters no longer race with deleting or re-enabling an invite
  - A configuration save no longer discards a concurrent membership write
  - Select fields no longer render overlapping dropdown arrows
  developer:
  - Bump Base to 2026.7.31
  - Rebuilt for Jellyfin 12.0 on .NET 10, with the target ABI raised to 12.0.0.0
  - The dashboard menu entry now shows a manage_accounts icon instead of the generic folder
  - CI builds, tests, and CodeQL now run on .NET 10, and label sync has the token scope it needs
